language: python
python:
    - "3.6"
    
install:
  - pip install -r requirements.txt
   
before_install:
    - pip install pytest pytest-cov
    - pip install coverage
    - pip install coveralls
  
script:
  #- pytest tests/.
  - coverage run --source='test_app.py test  
  - coverage report
    
script:
    - pytest
after_success:
    - coveralls
